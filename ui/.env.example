# Local dev: leave empty — Vite proxy forwards /api/* to http://localhost:5000
VITE_API_BASE_URL=

# Production: set this in your Vercel project settings
# Project Settings → Environment Variables → VITE_API_BASE_URL → https://your-backend.com
